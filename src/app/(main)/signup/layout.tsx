export function generateMetadata() {
    return {
        title: "Signup - ReCoron",
        description: "Create a ReCoron account to easily manage your scheduled tasks.",
    };
}

export default function SignupLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}